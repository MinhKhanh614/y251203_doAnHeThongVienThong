#pragma once

namespace MK_flag {
  extern volatile bool flagSendSMS;
  extern volatile bool flagCheckIMEI;
  extern volatile bool flagGPS;
  extern volatile bool flagBalance;
  extern volatile bool flagAT;
}